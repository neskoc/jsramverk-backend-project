language: node_js

node_js: "node"
node_js_14: "14"

services: mongodb

sudo: false

git:
    submodules: false

before_script:
    - npm install

script:
    - npm test

after_success:
    - npm run report-coveralls
    - npm run report-codecov
